# 小白官网（v0.5.0 · 登录注册同步版）GitHub Pages 发布指引

> 适用仓库：XSVICYYDS / Smart-Desktop-Pet-White  
> 官网最终部署地址：https://xsvicyyds.github.io/Smart-Desktop-Pet-White/  
> 登录注册页面地址（部署完成后）：https://xsvicyyds.github.io/Smart-Desktop-Pet-White/#/auth

---

## 📌 本次发布内容（与桌面端小白账号体系 100% 同步）

### 🌟 新增功能 1：统一账号系统
- ✅ 网站端新增 `/auth` 登录/注册页，**与桌面端小白登录对话框规格完全一致**
  - UI：粉色玻璃态卡片 + 小白动图 Logo + 「登录 / 注册」Tab 切换
  - 登录：邮箱 + 密码 + 4 位图形验证码 + 记住登录状态
  - 注册：昵称(2–20字符) + 邮箱 + **6 位邮箱验证码**(60秒限流 + 5分钟有效)
    + 密码强度条(≥8位+大小写字母+数字) + 确认密码 + 图形验证码 + **拼图滑块验证**
- ✅ 连续图形验证码错误 ≥ 2 次，自动升级为「拼图滑块验证」(容差 5 像素，失败 800ms 自动回弹)
- ✅ 会话持久化：勾选「记住登录状态」= 7 天有效；不勾选 = 1 天有效
- ✅ 导航栏联动：登录后桌面端(md+)显示昵称头像下拉，含
  - 👤 当前用户信息头（昵称、邮箱、"网站·桌面端 已同步登录"）
  - ☁️ 同步数据（预览）— 与桌面端托盘菜单「同步数据（预览）」一致
  - 🚪 退出登录 — 一键清理本地与桌面端同步的会话 token

### 🛠 发布工程化
- ✅ `vite.config.ts` 已配置 `base: '/Smart-Desktop-Pet-White/'`，GitHub Pages 资源路径与仓库子路径完全匹配
- ✅ `.github/workflows/deploy.yml` 自动部署管线已就绪：
  1. `push` 到 `main` / `master` 分支 → 自动执行 `npm install && npm run build`
  2. 自动追加 `dist/.nojekyll`，防止 GitHub Jekyll 忽略 _ 开头的资源
  3. 自动调用 `actions/deploy-pages@v4` 发布到官方 GitHub Pages 环境
- ✅ 本地构建已生成 `dist/` 目录并额外附带 `404.html`(= index.html 内容)，兼容 HashRouter 刷新回退

---

## 🚀 发布方式一：自动发布（推荐，最快 2 步）

### 第 1 步 — 提交并推送代码（用 GitHub Desktop）
仓库工作区当前变更清单（**全部需要提交**）：

```
✅ src/App.tsx                            （修改：新增 /auth 路由）
✅ src/components/Navbar.tsx              （修改：新增登录按钮+登录后菜单）
🆕 src/components/CaptchaCanvas.tsx       （新增：4位字母数字图形验证码）
🆕 src/components/SliderCaptcha.tsx       （新增：5px容差拼图滑块验证）
🆕 src/lib/authClient.ts                  （新增：与桌面端同规格的认证核心）
🆕 src/pages/Auth.tsx                     （新增：登录/注册 Tab 页面）
✅ dist/ 目录（构建产物，可选 — 自动部署不依赖它）
```

**GitHub Desktop 操作步骤（请您手动操作）：**
1. 打开 **GitHub Desktop**，确认仓库选中的是 `XSVICYYDS / Smart-Desktop-Pet-White`
2. 左下角「Summary（必填）」填写：
   ```
   feat(auth): 网站新增与桌面端完全同步的登录/注册功能
   ```
3. 「Description（可选）」可贴这段，方便以后回溯：
   ```
   - 新增 /auth 登录注册页，UI/校验完全对齐桌面端登录对话框
   - 4位图形验证码 + 6位邮箱验证码(60s限流/5min有效) + 拼图滑块(5px容差)
   - 会话remember_me=7天(不勾=1天)，用户数据结构与桌面端AuthSystem一致
   - Navbar新增登录按钮，登录后显示昵称头像下拉，支持同步数据预览+退出登录
   - vite.config.ts base=/Smart-Desktop-Pet-White/ 已适配 GitHub Pages 子路径
   - .github/workflows/deploy.yml 自动部署：push到main即自动npm run build+deploy pages
   ```
4. 点击蓝色按钮 **「Commit to main」**（或 master，看您分支名）
5. 顶部导航点击 **「Push origin」** → 推送完成 ✅

### 第 2 步 — 触发 GitHub Actions 自动部署并查看进度
推送成功后，自动部署会**立刻自动开始**：
1. 浏览器打开仓库 → https://github.com/XSVICYYDS/Smart-Desktop-Pet-White/actions
2. 顶部出现一条橙色运行中流水线，名称为 `Deploy to GitHub Pages`
3. 约 **1.5–3 分钟**后，流水线显示绿色 ✅ → 代表部署成功
4. 部署完成后访问官网：
   - 首页：https://xsvicyyds.github.io/Smart-Desktop-Pet-White/
   - 登录注册页：https://xsvicyyds.github.io/Smart-Desktop-Pet-White/#/auth

> ⚠️ 如果 Actions 页面没有运行记录，请到仓库 **Settings → Pages** 检查：
> **Source = GitHub Actions**（而不是 Deploy from branch），选择后下次 Push 就会自动跑。

---

## 🛠 发布方式二：手动部署（如果您不想等 Actions，GitHub Desktop 最快方式）

### 第 1 步 — 本地已构建（此文档生成时已执行完成 ✅）
构建产物在：
```
C:\Users\XS\Desktop\尚志中学809班徐慎智能桌面宠物小白\dist\
```
包含：
- index.html（入口，base=/Smart-Desktop-Pet-White/）
- 404.html（= index.html，HashRouter 刷新兼容）
- .nojekyll（禁用 Jekyll 处理）
- assets/ 打包好的 JS/CSS 与图片
- favicon.png、installer/portable 图标、xiaobai-logo.gif、gif/

### 第 2 步 — 用 GitHub Desktop 切到 gh-pages 分支并覆盖 dist 内容
1. GitHub Desktop → 菜单栏 **Repository → Show in Explorer** 打开仓库根目录
2. 在 GitHub Desktop 顶部切分支：
   - 若已有 `gh-pages` 分支：直接切换到它
   - 若无：点击 **「Current branch」→ 「New branch」→ 输入 `gh-pages` → 基于 main 创建
3. 切换到 `gh-pages` 分支后，把本地 `dist` 文件夹里的**全部内容**复制到仓库根目录（index.html、assets、gif、.nojekyll…都直接放在仓库根，不是 dist 子目录）
4. 回到 GitHub Desktop → Summary 填：
   ```
   deploy(gh-pages): 发布登录注册同步版官网 v0.5.0
   ```
5. **Commit to gh-pages** → **Push origin**
6. 仓库 **Settings → Pages → Source = Deploy from branch → Branch = gh-pages / root** → Save
7. 约 30 秒后 GitHub Pages 线上生效 ✅

---

## ✅ 部署完成后请您手动验收 5 个点（只需 1 分钟）

1. **首页** https://xsvicyyds.github.io/Smart-Desktop-Pet-White/
   - 右上角(桌面端≥1280px)看到「登录 / 注册」按钮
   - 移动端能在汉堡菜单里看到「登录 / 注册」
2. **点击登录按钮** → 跳转到 `#/auth`，默认登录 Tab，看到：
   小白 Logo + "欢迎回到小白官方网站" + 邮箱/密码/图形验证码/记住登录复选框
3. **切到注册 Tab** → 看到：
   昵称 / 邮箱 / 发送验证码按钮 / 密码(强度条) / 确认密码 / 图形验证码 / 滑块拼图（拼图块背景粉色，拖动验证）
4. **注册体验**（开发版模拟，不会真发邮件）：
   - 昵称：小白用户
   - 邮箱：test@xiaobai.com
   - 先点图形验证码刷新，再点「发送验证码」
     — 页面下方提示「模拟邮箱服务：本次验证码为 XXXXXX」
   - 复制 6 位验证码填入，密码 = `Abc123456`，确认密码同样
   - 输入右侧 4 位图形码（不区分大小写，点图可刷新）
   - 把滑块拖到缺口位置（5px 内都算过）
   - 点击「创建账号」→ 成功 Toast → 切到登录 Tab
5. **登录体验**：用刚才注册的 `test@xiaobai.com` / `Abc123456`，输入图形码 → 登录
   → 右上角显示昵称头像，点击下拉可看到"同步数据（预览）"和"退出登录"
   → 刷新页面后仍然保持登录状态 ✅

---

## 📜 与桌面端的数据同步原理（给您做个简单说明，不用操作）

- **密码哈希**：双方均使用 **PBKDF2-SHA256（10 万轮迭代，随机 16 字符 salt）**，存储格式 `salt_b64$iters$hash_b64`，今后您接入真实后端时，两端账号密码可以直接互通。
- **会话 token 结构**：`xb.<base64 payload>.<sign>`，载荷字段与桌面端完全一致（user_id / email / nickname / iat）。
- **升级策略**：图形验证码连续错误 ≥ 2 次，两边都会强制要求先通过「拼图滑块」。
- 目前为前端离线存储（localStorage），之后只需把 [authClient.ts](file:///c:/Users/XS/Desktop/尚志中学809班徐慎智能桌面宠物小白/src/lib/authClient.ts) 中 `_local*` 函数替换为真实 REST API 调用，前端其它代码一行都不用改。

🎉 推送成功后就等 Actions 绿灯亮起，您的官网登录/注册功能就正式上线啦！
